model TrackPoint {
  id        Int       @id @default(autoincrement())
  record    RunRecord @relation(fields: [recordId], references: [id], map: "track_point_runrecordid_fkey")
  recordId  String
  lat       Float
  lng       Float
  timestamp BigInt // unix 毫秒

  @@map("track_point")
}
