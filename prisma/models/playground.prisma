enum PlayGroundVisibility {
  PRIVATE
  PUBLIC
}

model PlayGround {
  id                   String                @id
  name                 String
  visibility           PlayGroundVisibility
  users                PlayGroundUser[]
  createdAt            DateTime              @db.Timestamp(3)
  updatedAt            DateTime              @db.Timestamp(3)
  playGroundCallRecord PlayGroundCallRecord?

  @@map("playground")
}
