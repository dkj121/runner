model User {
  id            String    @id
  name          String
  email         String    @unique
  emailVerified Boolean
  image         String?
  sessions      Session[]
  accounts      Account[]
  createdAt     DateTime  @db.Timestamp(3)
  updatedAt     DateTime  @db.Timestamp(3)

  @@map("user")
}
